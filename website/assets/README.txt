Drop your real images into this folder with these exact names
and the site will pick them up automatically (no code changes):

  logo.png     -> nav, footer, loading screen  (WSE AI Lab logo)
  me.png       -> home hero, about page        (founder photo, portrait ~4:5)
  example.png  -> all product card/detail images (landscape ~16:10)

Until a file exists, the site shows a branded gold/navy placeholder
in its place, so nothing looks broken meanwhile.
